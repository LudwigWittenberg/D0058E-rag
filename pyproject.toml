[project]
name = "chatbot"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "chromadb>=1.5.9",
    "crewai>=0.193.2",
    "flask>=3.1.3",
    "google-generativeai>=0.8.6",
    "hypothesis>=6.167.1",
    "jupyter>=1.1.1",
    "litellm>=1.74.9",
    "numpy>=2.5.2",
    "openai>=2.54.0",
    "pypdf2>=3.0.1",
    "pytest>=9.1.1",
    "python-dotenv>=1.2.3",
    "sentence-transformers>=6.0.1",
]

[tool.pyright]
# 'ai' and 'web' live inside rag-app/, which is added to sys.path at runtime
# (rag-app/run.py, LAB2_RAG/test_vectorstore.py). Tell the language server too.
extraPaths = ["rag-app"]
